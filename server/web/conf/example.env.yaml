app:
  name:
  version:
  title:
  domain:
  debug:
  location:
  language: zh
  node:

server:
  name:
  address:
  port:

database:
  orm:
    default:
    migration:
      dir:
      table:
    connections:
      default:
        driver:
        username:
        password:
        host:
        port:
        db:
        charset:
        prefix:
        log_mode:
  redis:
    default:
    connections:
      default:
        driver:
        host:
        port:
        username:
        password:
        db:

filesystem:
  default:
  disks:
    default:
      driver:
      access:
      secret:
      region:
      bucket:
      domain:
      endpoint:
      prefix:
      ssl:

jwt:
  secret:
  sub: web
  leeway:
  lifetime:
  refresh:
    mode:
    lifetime:
    leeway:
